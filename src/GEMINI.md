# Antigravity Harness — Tier Router v2.1
> Globally loaded policy router for Google Antigravity and Gemini CLI.
> Conditional skill loading is policy-only and unverified until a live restart canary proves it.

## Capability Reality

- Do not infer tool, hook, model, skill, or checker availability from this file.
- Record only current live observations; schema exposure is not behavioral proof.
- Preserve the same scope and verification requirements when preferred native
  surfaces are unavailable.

## Tier 0

For short read-only explanations, searches, and inspections, follow
`AGENTS.md`. Do not load the full Tier 1 pack unless the task needs it.

## Tier 1 Routing

Before mission-backed, multi-step, write, delegated, resumed, security,
migration, merge, deployment, publication, destructive, or high-risk work,
read this skill completely:

```text
$env:USERPROFILE\.gemini\config\skills\reliable-maker-control\SKILL.md
```

The source path is `src/skills/reliable-maker-control/SKILL.md`.
If the canonical entry is unavailable, use the preserved legacy
`$env:USERPROFILE\.gemini\skills\reliable-maker-control\SKILL.md` explicitly.
For a Codex handoff, also read the cross-engine-handoff skill. It cannot reduce
Antigravity's required checks, fresh evidence or native completion requirements.

After reading, state that Tier 1 policy was actually loaded. If the skill was
not read, report Tier 1 as inactive/unverified; never claim automatic loading.

## Authority

- Script enforcement applies only when the relevant script is invoked.
- Source-only completion evidence remains `checking` without trusted external
  checker attestation.
- The user retains scope expansion, destructive action, migration, merge,
  deployment, publication, and release authority.
