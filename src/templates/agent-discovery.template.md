# Antigravity project rule entry

Read the existing project `.agent/rules.md` as the canonical project policy.
This file is only a discovery pointer, not a second copy of that policy.
Do not overwrite neighboring Codex instructions. Explicit collaboration uses
the cross-engine-handoff skill and strict Antigravity adapter. All original
required checks, fresh hashes, guarded constraints and completion gate remain
binding, including when Codex reviews the result. Discovery is policy-only
until the installed Desktop version has actually loaded this rule.
