# Current context — Antigravity Desktop Harness v4.0 scoped use

## Publication snapshot (2026-09-10)

The user authorized this new repository and its explanation documents. This
snapshot excludes the original local Git history, personal paths and raw session
identifiers. Historical local evidence remains private; tests/fixtures/v351 is a
synthetic public correlation-shape fixture, not a byte-exact original log.
The no-commit/freeze statements below describe prior local tasks and do not block
this explicitly requested publication. Publishing does not enable new Desktop
hooks, change guard modes or prove native activation.

## 2026-09-10 asymmetric collaboration upgrade

The user approved collaboration with Codex while explicitly preserving all
Antigravity strong verification. This supersedes the baseline feature freeze
only for the bounded collaboration and discovery upgrade. See
docs/agent-collaboration.md. Baseline installed/live statements below describe
the original v4 run, not new runtime or Desktop evidence for this upgrade.

## Identity and boundaries

This repository is the sole source for the Google Antigravity Desktop reliability
and governance harness. Target: existing .gemini runtime and the existing
antigravity-reliable-control plugin. PowerShell helpers do not imply interchangeable
Desktop, IDE, CLI or SDK configuration. No other engine runtime is a delivery target.

## Current verified facts

- Initial source/runtime inventory matched 71/71 files (65 runtime + 6 plugin).
- Historical v3.5.1 case-02 output SHA256 and terminal Attempt matched the supplied
  record; read-only session audit returned Pre=3, Post=1, denied=2, missing/orphan=0.
  Originals were not rerun or changed. A bounded replay copy is in tests/fixtures/v351.
- v4 source work implements opt-in project policy, expiring/revocable/narrowing
  Binding, independent Tool Operation history, conservative Post classification,
  explicit completion references, optional Knowledge Gate and recoverable installers.
- Local tests and review evidence are recorded in artifacts/v4, with all RED and
  failed review snapshots preserved. Counts describe executed cases, not coverage.
- v4 candidate is installed: 76 normal runtime + 7 plugin files, zero hash mismatch.
  Final full regression passed 21/21 suites plus package and boundaries. Exact
  reports/manifests and remaining limits are in docs/v4-release.md.
- All five scoped lifecycle stages and the terminal/ADS refusal scenarios are
  live-observed. Both acceptance Attempts are now stopped; Binding remains revoked
  at revision 3. acceptance-02 uses a fixed output snapshot; acceptance-01 is unchanged.
- Scoped production decision: supervised low-risk reversible engineering assistance.
  See docs/production-readiness.md and artifacts/v4/production-closeout/readiness.json.
- Canonical completion is NOT closed: actual read-only Gate is unverified/claim-missing;
  the first Attempt's old evidence points to an output later changed by the test.
  That stale history is retained. Step 8 still lacks Post (cause unconfirmed), and
  the full session audit remains warning. No fake Claim or Post was generated.

## Non-negotiable limitations

Strict OS isolation is unavailable. Guarded can be bypassed by same-user arbitrary
execution or control-state modification outside observed tools; hashes and private
markers are not identity or permissions. Host TOCTOU is not closed.
Command output text never proves a child exit. Native Post completion is not task
acceptance. Valid source-only completion may reach checking; missing/stale evidence
remains unverified, as in this acceptance fixture.
Trusted checker authority, parent/child identity, and native Stop activation remain
unavailable/unverified unless separately backed by host evidence.
Knowledge media fixtures test unavailable capability, not real image/video semantics.
Real Obsidian installation has not been authorized or performed.

## Current execution contract

One unified plan: artifacts/plan_v4_convergence.md.
Requirement matrix and operator guide: docs/v4-requirements.md and docs/v4-operations.md.
No commit, push or PR. Preserve pre-existing dirty worktree and all history.
Historical snapshots: docs/changelog-v3.md.
Feature development is frozen. No reinstall or new business-workspace opt-in was
needed during closeout; future fixes require an actual reproducible use blocker.
